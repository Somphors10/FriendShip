# To all my friends

A simple page with photos and a letter to my friends.

Add more pictures by dropping them into `src/assets` (jpg, png, or webp). They will show up in the auto-scrolling gallery.

```bash
npm install
npm run dev
```
